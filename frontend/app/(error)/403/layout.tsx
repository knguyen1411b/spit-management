import { ReactNode } from 'react'
import { Metadata } from 'next'

export const metadata: Metadata = { title: 'Không có quyền truy cập - Spit Management' }

const layout = ({ children }: { children: ReactNode }) => children

export default layout
