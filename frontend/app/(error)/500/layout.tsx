import { ReactNode } from 'react'
import { Metadata } from 'next'

export const metadata: Metadata = { title: 'Lỗi máy chủ - Spit Management' }

const layout = ({ children }: { children: ReactNode }) => children

export default layout
